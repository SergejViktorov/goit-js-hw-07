const calc
const categories = document.querySelector('.item');
const title = categories.querySelector('h2')










console.log('Категория:', title.textContent);

const quantityElement = categoriesTitle.children;
console.log('Количество элементов:', quantityElement.length)
